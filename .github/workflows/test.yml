name: Node CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Testful Test
    steps:
      - uses: actions/checkout@v1
      - name: npm install and test
        run:
          npm i
          ls
          echo "This is before"
          npm run runner
          echo "This is after"
        env:
          CI: true