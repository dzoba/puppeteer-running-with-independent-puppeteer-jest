name: Node CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Testful Test
    steps:
      - name: npm install and test
        run:
          npm i
          npm run runner & sleep 10; npm run test:integration
        env:
          CI: true